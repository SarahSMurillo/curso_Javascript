/*Redondeo hacia arriba*/
let res;
res=Math.round(2.5);

res=Math.ceil(2.5);

/*Redonde hacia abajo*/
res=Math.floor(2.5);

/*Valor absoluto*/
res=Math.abs(-300);

/*Raíz cuadrada*/
res=Math.sqrt(2);

/*Min de una cadena*/
res=Math.min(2,5,44,7,-1,-50);

/*Max de una cadena*/
res=Math.max(2,5,44,7,-1,-50);

/*Num aleatorios*/
res=Math.random();

    //num aleatorios dentro de un rango
res=Math.floor(Math.random()*30);
res=Math.ceil(Math.random()*30);

console.log(res);